export type ColdEmailOffering = {
  id: string;
  user_id: string;
  title: string;
  hook: string;
  description: string;
  source_notes: string;
  sort_order: number;
  is_archived: boolean;
  created_at: string;
  updated_at: string;
};
